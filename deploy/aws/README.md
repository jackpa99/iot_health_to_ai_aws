# AWS Infrastructure (Historical Reference)

These CloudFormation templates and ECS task definitions were used for the
original AWS-hosted deployment.  They are retained as reference only.

The project has moved to a cloud-portable architecture using Kubernetes
manifests in `../k8s/`.
