# Placeholder for External Secrets Operator SecretStore.
# The platform team should replace this with the actual store configuration
# (e.g. Vault, AWS Secrets Manager, Azure Key Vault).
#
# apiVersion: external-secrets.io/v1beta1
# kind: SecretStore
# metadata:
#   name: vault
#   namespace: iot-health
# spec:
#   provider:
#     vault:
#       server: "https://vault.example.com"
#       path: "secret"
#       auth:
#         kubernetes:
#           mountPath: "kubernetes"
#           role: "iot-health"
