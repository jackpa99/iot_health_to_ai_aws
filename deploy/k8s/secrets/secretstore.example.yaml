# Placeholder SecretStore — the platform team fills in the upstream backend
# (HashiCorp Vault, Azure Key Vault, AWS Secrets Manager, GCP Secret Manager,
# Akeyless, Conjur, etc.). The application never changes; only this file does.
#
# Requires External Secrets Operator (ESO):
#   helm repo add external-secrets https://charts.external-secrets.io
#   helm install external-secrets external-secrets/external-secrets -n external-secrets --create-namespace
#
# Example shown here uses the `fake` provider for local testing; replace with
# your corp backend before shipping.
apiVersion: external-secrets.io/v1beta1
kind: SecretStore
metadata:
  name: corp-secret-store
  namespace: iot
spec:
  provider:
    # --- REPLACE THIS BLOCK WITH YOUR ACTUAL BACKEND ---
    # Vault example:
    # vault:
    #   server: "https://vault.corp.example.com"
    #   path: "kv"
    #   version: "v2"
    #   auth:
    #     kubernetes:
    #       mountPath: "kubernetes"
    #       role: "iot-model-trainer"
    #       serviceAccountRef:
    #         name: "model-trainer"
    #
    # Azure Key Vault example:
    # azurekv:
    #   vaultUrl: "https://corp-iot.vault.azure.net"
    #   authType: WorkloadIdentity
    #   serviceAccountRef:
    #     name: model-trainer
    #
    # Placeholder (works only for local dev / CI):
    fake:
      data:
        - key: model-store/access-key-id
          value: "minio"
        - key: model-store/secret-access-key
          value: "minio123"
